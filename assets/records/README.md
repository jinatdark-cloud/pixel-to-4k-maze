# Asset Records

This directory stores one Markdown record per generated asset.

Each record should include asset ID, family, prompt source, generated preview status, review notes, and the expected image path under `assets/images/`.
